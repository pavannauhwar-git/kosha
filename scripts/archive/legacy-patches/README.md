# Legacy Patch Scripts

These scripts were used for one-time cleanups, tactical patches, and diagnostics during previous iterations.

They are intentionally archived to keep the top-level `scripts/` folder focused on active tooling.

Do not run these in normal development workflows unless you intentionally need to reproduce an old migration/patch path.
